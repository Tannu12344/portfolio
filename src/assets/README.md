# assets/

Place static images, icons, and media referenced by components/data here
(e.g. `avatar.jpg`, project thumbnails). Empty in Phase 1 — `profile.js`
and `projects.js` currently reference `null` for images/avatars as
placeholders.
